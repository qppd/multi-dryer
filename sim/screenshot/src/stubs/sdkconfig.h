// sdkconfig.h — empty stand-in for the ESP-IDF generated config header.
// The real lvgl_v8_port.h includes this; on the host none of the CONFIG_*
// macros are defined, which is exactly what we want.
